{
  "name": "http-quest",
  "version": "1.0.0",
  "private": true,
  "description": "A browser game for learning HTTP and REST by building real requests against a real API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "on-headers": "^1.1.0"
  }
}
